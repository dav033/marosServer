on:
  schedule:
    - cron: '*/14 * * * *'   # cada 14 minutos
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render app
        run: curl -fsS https://marosserver.onrender.com/ || exit 1
